# 🛠 tasks.md — Implementation Plan（落地实施）

> 📌 **Metadata**
> - Spec Branch: `spec/<feature-or-bug>`
> - All commits must be pushed to this branch (not `main`).
> - ✅ Each **Phase completion** requires a Git commit following [Git-Flow.md](../Git-Flow.md).
> - ✅ All task statuses must be updated in real time: `[]` (default), `[-]` (doing), `[x]` (done)

---

## 📊 Phases Overview

列出本 spec 的所有阶段（可增减）及其目的：

- Phase 1: `<标题>` — `<简要说明>`
- Phase 2: `<标题>` — `<简要说明>`
- Phase 3: `<标题>` — `<简要说明>`
- Phase N: `<标题>` — `<简要说明>`

---

## 📍 Phase 1: `<标题>`

> 🎯 **目标**：`<概括该阶段的核心目标>`

- [ ] 1. `<任务组标题>`
  - **Summary:** `<任务说明>`
  - `path/to/file1` — `<修改xxx方法 xxx字段等>`
  - `path/to/fileN` — `<修改xxx方法 xxx字段等>`
  - _Requirements: 2.1, 2.2, 2 `<对应的需求/约束说明>`
  - 提交本次修改的代码（commit: `feat(phase1): <描述>`）

- [ ] N. `<任务组标题>`
  - **Summary:** `<任务说明>`
  - `path/to/file1` — `<修改xxx方法 xxx字段等>`
  - `path/to/fileN` — `<修改xxx方法 xxx字段等>`
  - _Requirements: 2.1, 2.2, 2 `<对应的需求/约束说明>`
  - 提交本次修改的代码（commit: `feat(phase1): <描述>`）



---
